#!/usr/bin/env zsh

# These are some completion scripts that are
# generated by the programs themselves.
# To reduce startup time, I am statically inserting
# them into the startup file.

# pip
pip completion --zsh

# antibody
cat <<'EOF'
function _antibody {
	IFS=' ' read -A reply <<< "help bundle update home purge list init"
}
compctl -K _antibody antibody
EOF
