cd ${0%/*} || exit 1
sources=(
    completion
    directories
    history
    key-bindings
    git
)

rm -v *.dl.zsh(N)
for src in $sources; do
    wget -qO "$src.dl.zsh" 'https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/lib/'$src'.zsh'
done

cd $OLDPWD
unset sources
