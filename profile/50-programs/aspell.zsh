# Aspell

# Change the aspell config directory to ~/.config/aspell/
# and the config file name to aspell.conf.
# The default are multiple ~/.aspell files.
export ASPELL_CONF="home-dir $XDG_CONFIG_HOME/aspell; per-conf aspell.conf"
