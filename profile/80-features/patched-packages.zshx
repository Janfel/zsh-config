#!/usr/bin/env zsh
# Patched Packages

typeset -A assoc
assoc=(python-black black)

# All installed packages belonging to the “jfl-patched” group.
pkgs=(${${(f)"$(pacman -Qg jfl-patched)"}#* })
for pkg in $pkgs; do
	pkg=${pkg%-git}
	printf 'patched_packages+=%s\n' ${(qq)assoc[$pkg]:-$pkg}
done
